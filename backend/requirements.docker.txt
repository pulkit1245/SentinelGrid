# ── SentinelGrid Backend Requirements (Docker-optimised, fully pinned) ─────────
# CPU-only PyTorch index (avoids 5GB CUDA download, ~200MB instead)
--extra-index-url https://download.pytorch.org/whl/cpu
#
# All grpc-related packages pinned to the same version to eliminate
# pip resolver backtracking. sentence-transformers removed (pulls 5GB CUDA).

fastapi==0.109.0
uvicorn==0.27.0.post1
pydantic==2.5.3
pydantic-settings==2.1.0
networkx==3.2.1

# grpc stack — all pinned to same version so no backtracking
grpcio==1.62.3
grpcio-status==1.62.3
grpcio-tools==1.62.3
qdrant-client==1.7.3

# ── LangChain 0.2.x ecosystem (compatible with langchain-core 0.2.x) ──────────
langchain==0.2.16
langchain-core==0.2.40
langchain-community==0.2.16
langchain-openai==0.1.23
langchain-google-genai==1.0.10
langgraph==0.2.4

# ── Sentence Transformers (CPU-only torch, ~200MB not 5GB) ───────────────────
torch==2.2.2+cpu
sentence-transformers==2.7.0

neo4j==5.16.0
sqlalchemy==2.0.25
asyncpg==0.29.0
psycopg2-binary==2.9.9
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
pytest==7.4.4
pytest-asyncio==0.23.4
httpx==0.26.0
python-dotenv==1.0.0
PyPDF2==3.0.1
rank-bm25==0.2.2
redis==5.0.1
sentry-sdk==1.40.0
email-validator==2.2.0
alembic==1.13.1
numpy==1.26.3
scikit-learn==1.4.0
fakeredis==2.21.0
